<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .header img {
      max-width: 200px;
    }
    .content {
      background: #f9f9f9;
      padding: 20px;
      border-radius: 5px;
    }
    .confirmation-icon {
      text-align: center;
      font-size: 48px;
      color: #1976d2;
      margin: 20px 0;
    }
    .event-info {
      margin: 20px 0;
      padding: 15px;
      background: #fff;
      border-radius: 5px;
      border: 1px solid #eee;
    }
    .registration-details {
      margin: 20px 0;
      padding: 15px;
      background: #fff;
      border-radius: 5px;
      border: 1px solid #eee;
    }
    .action-button {
      display: inline-block;
      background-color: #1976d2;
      color: white;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 4px;
      font-weight: bold;
      margin: 20px 0;
    }
    .footer {
      text-align: center;
      margin-top: 30px;
      font-size: 12px;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <img src="{{logoUrl}}" alt="FGC Logo">
    </div>
    
    <div class="content">
      <div class="confirmation-icon">✓</div>
      
      <h2>Inscrição Confirmada!</h2>
      
      <p>Olá {{name}},</p>
      
      <p>Sua inscrição no evento <strong>{{eventName}}</strong> foi recebida com sucesso!</p>
      
      <div class="event-info">
        <h3>Detalhes do Evento</h3>
        <p><strong>Nome do Evento:</strong> {{eventName}}</p>
        <p><strong>Data:</strong> {{eventDate}}</p>
        <p><strong>Local:</strong> {{eventLocation}}</p>
        <p><strong>Horário:</strong> {{eventTime}}</p>
      </div>

      <div class="registration-details">
        <h3>Detalhes da Inscrição</h3>
        <p><strong>Protocolo:</strong> {{protocol}}</p>
        <p><strong>Status:</strong> {{status}}</p>
        {{#if modalityName}}
        <p><strong>Modalidade:</strong> {{modalityName}}</p>
        {{/if}}
        {{#if categoryName}}
        <p><strong>Categoria:</strong> {{categoryName}}</p>
        {{/if}}
        {{#if price}}
        <p><strong>Valor:</strong> R$ {{price}}</p>
        {{else}}
        <p><strong>Participação:</strong> Gratuita</p>
        {{/if}}
      </div>
      
      {{#if eventURL}}
      <div style="text-align: center;">
        <a href="{{eventURL}}" class="action-button">Ver Detalhes do Evento</a>
      </div>
      {{/if}}
      
      <p>Agradecemos sua participação e estamos ansiosos para recebê-lo no evento!</p>
    </div>

    <div class="footer">
      <p>Este é um e-mail automático, por favor não responda.</p>
      <p>Federação Goiana de Ciclismo - {{year}}</p>
    </div>
  </div>
</body>
</html>
