import { PaymentStatus } from "./types"

export function mapTransactionStatusToPaymentStatus(status: PaymentStatus): PaymentStatus {
  return status
}

export function mapPaymentStatusToTransactionStatus(status: PaymentStatus): PaymentStatus {
  return status
}
